<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css?family=Poppins"
      rel="stylesheet"
    />

    <style>
      * {
        margin: 0;
        padding: 0;
        font-family: "Poppins", Helvetica, sans-serif;
      }

      .noPost {
        color: #111111;
        font-size: 16px;
        font-weight: 600;
      }

      .container {
        width: 100%;
        height: 100%;
        min-height: 100vh;

        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;

        background-color: #eeeeee;
      }

      .header {
        width: 70%;
        height: 80px;

        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: flex-end;
      }

      .hero {
        width: 70%;
        height: 350px;

        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }

      .hero h1 {
        color: #111111;
        font-size: 28px;
        font-weight: 600;
        margin-bottom: 24px;
      }

      .hero p {
        color: #444;

        margin-bottom: 36px;
      }

      .hero .search {
        width: 100%;
        height: auto;

        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;

        gap: 16px;
      }

      .posts {
        width: 100%;
        height: auto;

        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;

        gap: 24px;

        padding-bottom: 64px;
      }

      .post {
        width: 100%;
        height: auto;
        max-width: 664px;
        min-width: 400px;

        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: space-between;

        padding: 16px 24px;
        border-radius: 5px;

        background: rgb(245, 245, 245);
      }

      .post h1 {
        color: #111111;
        font-size: 28px;
        font-weight: 600;
        margin-bottom: 8px;
      }

      .post p {
        color: #444;
        margin-bottom: 16px;
      }

      .post a {
        color: #222;
        font-weight: bold;

        text-decoration: underline;
        cursor: pointer;
      }

      .post span {
        color: #555;
        font-size: 12px;
        font-weight: bold;
      }

      input[type="text"] {
        width: 60%;
        height: 45px;

        border-radius: 4px;
        outline: none;
        border: none;

        background: #f4f4f4;
        padding: 2px 24px;
      }

      button {
        height: 45px;

        border-radius: 4px;
        outline: none;
        border: none;

        background: rgb(168 85 247);
        color: #fff;

        font-weight: bold;
        font-size: 14px;

        padding: 5px 24px;
        cursor: pointer;
      }

      button:hover {
        opacity: 0.8;
      }
    </style>
    <title>Blog</title>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <button type="button" onclick="goToRegister()">Novo Post</button>
      </div>

      <div class="hero">
        <h1>fgalmeida</h1>
        <p>Hello! Welcome to my development blog</p>
        <div>
          <form class="search" method="post">
            <input
              type="text"
              id="search"
              name="search"
              placeholder="Pesquisar"
            />
            <button type="submit">Pesquisar</button>
          </form>
        </div>
      </div>

      <div class="posts">
        <% if(posts.length == 0) { %>
        <p class="noPost">
          O post que você está procurando não foi encontrado 😞
        </p>
        <% } %> <% posts.forEach(function(post){ %>
        <div class="post">
          <h1><%= post.title %></h1>
          <p>
            <%= post.description.substring(0, 255) %>
            <a href="/post/<%= post.slug %>">ver mais</a>
          </p>
          <span><%= moment(post.created_at).locale('pt-br').fromNow() %></span>
        </div>
        <% }) %>
      </div>
    </div>
    <script>
      function goToRegister() {
        window.location.href = "/post/new";
      }
    </script>
  </body>
</html>
